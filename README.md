# recipe-app-api

### I stopped at SECTION 14!!! Continue from THERE

for Udemy Django rest API course

after creating a model we do migration
docker compose run --rm app sh -c "python manage.py makemigrations"

after adding a new package
we do docker-compose build
